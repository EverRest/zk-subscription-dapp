export type Subscription = {
  name: string;
  tier: string;
  description: string;
  price: string;
  image: string;
};

export type Content = {
  title: string;
  body: string;
  tags: string[];
};
